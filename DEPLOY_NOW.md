# 🚀 立即部署指南

## 当前状态

✅ **所有代码已准备好！** Flask 已配置为同时提供前端和后端服务。

## 最简单的部署方式：Render（免费开始）

### 步骤 1: 确保代码已推送到 GitHub

```bash
cd /home/yw28498/specialty-chatbot/RAG/rag-parameter-tuner
git status  # 检查是否有未提交的更改
git push origin main  # 确保所有代码都已推送
```

### 步骤 2: 在 Render 创建 Web Service

1. **访问 [render.com](https://render.com)**
   - 使用 GitHub 账号登录（推荐）

2. **创建新服务**
   - 点击 "New +" → "Web Service"
   - 选择 "Connect GitHub repository"
   - 选择仓库：`nina510/rag-parameter-tuner`

3. **配置服务**
   - **Name**: `rag-parameter-tuner`（或你喜欢的名字）
   - **Environment**: `Python 3`
   - **Region**: 选择离你最近的区域
   - **Branch**: `main`
   - **Root Directory**: 留空（或填写 `RAG/rag-parameter-tuner` 如果仓库在子目录）

4. **构建和启动命令**
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120`

5. **环境变量**
   点击 "Advanced" → "Add Environment Variable"，添加：
   - **Key**: `OPENAI_API_KEY`
   - **Value**: 你的 OpenAI API 密钥（从 [platform.openai.com/api-keys](https://platform.openai.com/api-keys) 获取）
   
   可选环境变量：
   - **Key**: `FLASK_DEBUG`
   - **Value**: `False`
   
   - **Key**: `PORT`
   - **Value**: 留空（Render 会自动设置）

6. **实例类型**
   - 选择 **Free**（免费层，有休眠限制）
   - 或选择付费计划（无休眠限制）

7. **点击 "Create Web Service"**

### 步骤 3: 等待部署完成

- Render 会自动：
  1. 克隆你的 GitHub 仓库
  2. 安装依赖（`pip install -r requirements.txt`）
  3. 启动服务（`gunicorn app:app ...`）

- 部署时间：约 5-10 分钟（首次部署）

- 部署完成后，你会看到：
  - ✅ "Live" 状态
  - 🌐 你的应用 URL（如：`https://rag-parameter-tuner.onrender.com`）

### 步骤 4: 访问应用

直接访问 Render 提供的 URL，例如：
```
https://rag-parameter-tuner.onrender.com
```

**现在前端和后端都在同一个 URL 上！**
- 前端页面：`https://rag-parameter-tuner.onrender.com/`
- API 端点：`https://rag-parameter-tuner.onrender.com/api/*`

### 步骤 5: 测试功能

1. 打开应用 URL
2. 测试生成答案功能
3. 检查浏览器控制台是否有错误
4. 测试不同的参数组合

## 📝 重要提示

### 免费层的限制（Render Free）

- ⚠️ **休眠机制**：如果 15 分钟没有请求，服务会休眠
- ⚠️ **首次唤醒**：休眠后首次请求可能需要 30-60 秒来唤醒
- ✅ **解决方案**：使用付费计划，或接受这个限制

### 如果遇到问题

1. **查看日志**
   - 在 Render 控制台点击 "Logs" 标签
   - 查看构建和运行日志

2. **常见问题**
   - **构建失败**：检查 `requirements.txt` 是否包含所有依赖
   - **启动失败**：检查 `Procfile` 或启动命令是否正确
   - **API 错误**：检查 `OPENAI_API_KEY` 环境变量是否正确设置

3. **检查代码**
   ```bash
   # 本地测试
   cd /home/yw28498/specialty-chatbot/RAG/rag-parameter-tuner
   python3 app.py
   # 访问 http://localhost:5000
   ```

## 🎯 部署检查清单

- [ ] 代码已推送到 GitHub
- [ ] 在 Render 创建了 Web Service
- [ ] 配置了 `OPENAI_API_KEY` 环境变量
- [ ] 部署成功（显示 "Live" 状态）
- [ ] 可以访问应用 URL
- [ ] 前端页面正常显示
- [ ] API 调用正常工作

## 🔄 更新部署

当你修改代码后：

1. **提交并推送**
   ```bash
   git add .
   git commit -m "Update: 描述你的更改"
   git push origin main
   ```

2. **Render 会自动重新部署**
   - Render 会检测到 GitHub 推送
   - 自动触发新的构建和部署
   - 约 5-10 分钟后更新完成

## 📚 其他部署选项

如果不想用 Render，也可以使用：

- **Railway**: [railway.app](https://railway.app) - 按使用量计费
- **Fly.io**: [fly.io](https://fly.io) - 有免费额度
- **Heroku**: [heroku.com](https://heroku.com) - 需要信用卡（有免费层）

部署步骤类似，主要区别是环境变量配置和启动命令。

